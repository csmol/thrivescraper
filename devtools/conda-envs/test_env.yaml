name: test
channels:

  - defaults
dependencies:
  # Base depends
  - python
  - pip

  - pygithub
  - requests

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  #- pip:
  #  - codecov

