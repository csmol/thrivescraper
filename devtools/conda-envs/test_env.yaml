name: test
channels:

  - defaults
dependencies:
  # Base depends
  - python
  - pip

  - beautifulsoup4
  - pygithub
  - requests

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
    # Documentation
    - sphinx-copybutton
    - sphinxnotes-strike

  # Pip-only installs
  #- pip:
  #  - codecov

